import React from "react";
import { StatusBar } from "expo-status-bar";
import { StyleSheet, Text, View } from "react-native";

export const Buffer = ({ height }) => {
  return <View style={{ height }}></View>;
};
